# SSH Console - Web-based Multi-terminal Manager

Мощный веб-интерфейс для управления SSH-соединениями, Docker-контейнерами и файловой системой серверов. Построен на базе Vert.x (Java) и React с акцентом на высокую производительность и удобство мульти-терминальной работы.

## 🚀 Основные возможности

### Backend (Vert.x & SSH)
- **Мультиплексирование сессий**: Оптимизация ресурсов за счет переиспользования одного SSH-соединения для открытия до 10 виртуальных терминалов (каналов). При превышении лимита автоматически открывается новое SSH-соединение.
- **Восстановление состояний**: Состояние всех открытых сессий сохраняется в **Redis**. После перезагрузки страницы или сервера, сессии автоматически восстанавливаются (включая историю вывода из буфера).
- **Индивидуальные лимиты**: Настраиваемое ограничение максимального количества подключений для каждого пользователя в рамках одного сервера.
- **Docker API через SSH**: Управление контейнерами (список, логи, рестарт, статистика) без необходимости открывать Docker-порт наружу — запросы проксируются через SSH-туннель к локальному сокету `/var/run/docker.sock`.
- **File Manager**: Встроенный файловый менеджер для навигации, просмотра размеров директорий, создания папок и управления правами (chmod) через SSH.
- **Буферизация вывода**: Весь вывод терминала буферизируется на стороне бэкенда. Это гарантирует, что при переключении между вкладками или при синхронизации данных между окнами, история вывода подгрузится мгновенно и без потерь.

### Frontend (React & Xterm.js)
- **Синхронизация между вкладками**: Благодаря интеграции с Vert.x EventBus Bridge, состояние интерфейса (активные вкладки, режим просмотра) синхронизируется между всеми открытыми окнами браузера в реальном времени.
- **Drag & Drop интерфейс**: Полная поддержка перетаскивания:
  - Группировка терминалов по серверам.
  - Перемещение терминалов между группами и изменение их порядка.
  - Удобное управление панелями управления.
- **Продвинутый поиск**: Единое поле поиска для мгновенной фильтрации по:
  - Активным открытым сессиям.
  - Истории последних подключений.
  - Списку доступных серверов из конфигурации.
- **Мульти-режимный интерфейс**: Переключение между режимами "Терминал", "Docker" и "Файлы" для каждой сессии с автоматической синхронизацией состояния.
- **Навигация Вперед/Назад**: Поддержка истории переходов по путям в файловом менеджере и состояниям интерфейса.
- **Window Management**: Возможность "откреплять" терминалы (detach) для работы в многооконном режиме с сохранением общей синхронизации.

## 🛠 Технологический стек

- **Backend**:
  - **Java 17+**, **Vert.x** (EventBus, Web, Redis Client).
  - **JSch** для работы с SSH протоколом.
  - **Redis** для персистентного хранения сессий и метаданных.
- **Frontend**:
  - **React 18**, **Vite**.
  - **Xterm.js** для высокопроизводительной эмуляции терминала.
  - **Vert.x EventBus Bridge Client** для реального времени.

## 🏗 Архитектура

Проект использует событийно-ориентированную архитектуру на базе шины событий (EventBus). Бэкенд выступает в роли моста между SSH-каналами и WebSocket-соединениями фронтенда.

- **SshVerticle**: Управляет жизненным циклом SSH-сессий, следит за таймаутами простоя (по умолчанию 3 минуты активного ожидания перед переходом в режим "сна") и взаимодействует с Redis.
- **MainVerticle**: Точка входа, настраивает HTTP-сервер, роутинг статики и EventBus Bridge с авторизацией и разграничением прав.

## 📦 Установка и запуск

### Требования
- Java 17+
- Node.js 18+
- Redis Server (по умолчанию на `localhost:6379`)

### Запуск бэкенда
1. Перейдите в директорию `back`.
2. Настройте список серверов в `src/main/resources/servers.json`.
3. Соберите и запустите проект:
   ```bash
   ./gradlew run
   ```

### Запуск фронтенда
1. Перейдите в директорию `front`.
2. Установите зависимости:
   ```bash
   npm install
   ```
3. Запустите сервер для разработки:
   ```bash
   npm run dev
   ```